/*
var draw_ground = function(_value, _index) 
{
	if (_index == current_layer) draw_set_alpha(1);
	else draw_set_alpha(0.3);
	
	draw_line(-1, _value, room_width, _value);
	draw_set_alpha(1);
}

array_foreach(layer_ground, draw_ground);
*/